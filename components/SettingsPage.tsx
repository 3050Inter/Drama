"use client";
import {useEffect,useState} from "react";import {addEmployee,deactivateEmployee,getEmployees} from "@/lib/api";import type {Employee} from "@/lib/types";
export default function SettingsPage({onChanged}:{onChanged:()=>Promise<void>}){const [employees,setEmployees]=useState<Employee[]>([]),[name,setName]=useState("");const load=async()=>{const r=await getEmployees();if(r.ok)setEmployees(r.rows||[])};useEffect(()=>{load().catch(()=>{})},[]);const add=async()=>{const n=name.trim();if(!n)return;await addEmployee(n);setName("");await load();await onChanged()};const remove=async(id:string)=>{if(!confirm("직원을 비활성화할까요? 과거 기록은 유지됩니다."))return;await deactivateEmployee(id);await load();await onChanged()};return <div className="space-y-4"><div className="rounded-2xl bg-[#111A2E] p-4"><div className="text-lg font-black">직원 관리</div><div className="mt-3 flex gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="직원명" className="h-12 flex-1 rounded-xl bg-slate-800 px-4 text-white outline-none"/><button onClick={add} className="h-12 rounded-xl bg-green-500 px-4 font-black">추가</button></div></div><div className="space-y-2">{employees.map(emp=><div key={emp.id} className="flex items-center justify-between rounded-2xl bg-[#111A2E] p-4"><div><div className="font-black">{emp.name}</div><div className="text-xs text-slate-500">ID {emp.id}</div></div><button onClick={()=>remove(emp.id)} className="text-sm text-red-400">비활성화</button></div>)}</div><div className="rounded-2xl bg-[#111A2E] p-4 text-sm text-slate-400">앱 버전: V6.5 / 월별 통계 포함</div></div>}
