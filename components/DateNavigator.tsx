"use client";
import {CalendarDays,ChevronLeft,ChevronRight} from "lucide-react";import {addDays} from "@/lib/formatter";
export default function DateNavigator({date,onChange}:{date:string;onChange:(d:string)=>void}){return <div className="mt-4 flex items-center gap-2"><button onClick={()=>onChange(addDays(date,-1))} className="flex h-11 w-14 items-center justify-center rounded-2xl bg-[#111A2E]"><ChevronLeft size={22}/></button><label className="flex h-11 flex-1 items-center justify-center gap-2 rounded-2xl bg-[#111A2E] px-3 font-bold"><CalendarDays size={17}/><input type="date" value={date} onChange={e=>onChange(e.target.value)} className="w-full bg-transparent text-center text-white outline-none"/></label><button onClick={()=>onChange(addDays(date,1))} className="flex h-11 w-14 items-center justify-center rounded-2xl bg-[#111A2E]"><ChevronRight size={22}/></button></div>}
